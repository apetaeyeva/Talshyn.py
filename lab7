matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

# Вывод матрицы
for row in matrix:
    print(row)

# Сумма элементов матрицы
total = sum(sum(row) for row in matrix)
print("Сумма всех элементов:", total)
